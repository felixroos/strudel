import { SlideRepl } from '../SlideRepl.jsx';

# What is Strudel

- a JavaScript port of the popular music live coding language TidalCycles
- zero-install live coding environment for the browser
- free & open source, modular, hackable, sharable, embeddable:

<SlideRepl
  hideHeader
  fontSize={24}
  tune={`samples('shabda/speech:hello,paris,world')
samples('github:yaxu/clean-breaks')

p1: s("<hello <paris world>>").delay(.5).speed(rand.range(.5,1))
 
p2: note("[c a f e](<3 5>,8,<0 1>)".add("0,.1").sub(24))
  .s('sawtooth').lpf(600).lpenv(-2).lpa(.1)
 
p3: "[~ <Cm7 Fm7 Bb7 Db7>]*2".voicings('lefthand').note().clip(.2)
  .echoWith(6, .1, (p,n)=>p.add(note(n*.6)))
  .lpf(sine.range(300,1000).slow(8)).lpq(6)
  .add(note(perlin.range(0,.8))).s('gm_epiano2')
  .mask("<0 1>")
 
p4: s("riffin/2").fit().chop(16).sometimes(ply("2")).dec(.3)`}
/>

<div className="h-[600px]"/>
