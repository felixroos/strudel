import { SlideRepl } from '../SlideRepl.jsx';

# superdough synth

Subtractive:

<SlideRepl
  hideHeader
  fontSize={24}
  canvasHeight={100}
  tune={`note("<c eb g bb d>*4") // melody
  .s("sawtooth").lpf(600) // waveform + filter
  //.lpa(.3).lpenv(2) // filter envelope
  //.vib("4:.2") // vibrato
  //.room(1) // reverb
  //.stack(note("c2")).clip(2) // add bass + prolong notes
  .scope()`}
/>

FM:

<SlideRepl
  hideHeader
  fontSize={24}
  canvasHeight={100}
  tune={`note("<c eb g bb d>(<3 5>,8)*2").jux(rev)
  .s("sine")
  .fm("<4 8 2>*4").fmh("2 4").dec(.1)
  .lpf(500).lpa(.1).lpenv(-4)
  .room(.5).delay(.5)
  .scope()`}
/>

Wavetable:

<SlideRepl
  hideHeader
  fontSize={24}
  canvasHeight={100}
  tune={`await samples('bubo:waveforms')
n(run(8).fast(2)).s("wt_dbass").note("c2")
.lpf(perlin.range(200,2000).slow(4))
.lpenv(-3).lpa(.1).room(.5)
.scope()`}
/>

<div className="h-[600px]" />
