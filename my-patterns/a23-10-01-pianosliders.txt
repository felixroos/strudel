n(run(slider(.25).range(1,12).round()))
  .chord("<Cm D>/8")
  .jux(rev)
  .offset(slider(.5).range(-10,4).round())
  .voicing()
  .add(note(slider(0).range(0,12)))
  .lpf(slider(0.492).range(200,2000))
  .clip(slider(.1).range(0.01,4))
  .room(slider(.5).range(0,2))
  .delay(slider(0).range(0,1))
  .sometimesBy(slider(0), add(note(12)))
  .piano()